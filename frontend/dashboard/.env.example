# Copy this file to .env and adjust as needed
# Set PORT to avoid conflict with backend Docker services (which run on port 3000)
PORT=4000

# API base URL (used when not using the proxy feature)
REACT_APP_API_URL=http://localhost:3000

# WebSocket server URL
REACT_APP_WS_URL=ws://localhost:8080
